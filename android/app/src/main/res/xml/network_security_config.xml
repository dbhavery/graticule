<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is off everywhere by default, which is the setting a weather
  app should ship with: every public feed this app reads is available over TLS
  and there is no reason to accept a downgrade on someone's phone.

  The exceptions are private address space only, and they exist because a
  developer build has to be able to reach a backend that has no certificate:

    10.0.2.2      the Android emulator's fixed alias for the machine hosting it
    localhost     a backend port-forwarded over `adb reverse tcp:8744 tcp:8744`,
    127.0.0.1     which is how a REAL phone on a USB cable reaches the desktop

  A LAN address is deliberately NOT here. This file takes hostnames and IP
  literals, not ranges, so "192.168.1.0" would have been a hostname matching
  nothing: a rule that looks like it permits a home network while in fact
  permitting one address nobody uses. `adb reverse` covers the real-phone case
  without pretending.

  None of these is routable from the public internet. A shipped build talks to
  a deployed backend over HTTPS and never consults this list.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
